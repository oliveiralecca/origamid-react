import React from 'react' 

// criando um contexto
const UserContext = React.createContext()

// importa esse contexto na parte mais global da aplicação, ou numa parte qualquer onde quer que ele seja utilizado
export default UserContext
